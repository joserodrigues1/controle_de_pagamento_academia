# Porta do servidor (site + API juntos). Plataformas como Railway/Render definem PORT automaticamente.
# PORT=4000

# Só use se a API estiver em OUTRO domínio (caso raro; o padrão é tudo na mesma origem).
# VITE_API_BASE=/api
